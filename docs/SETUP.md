# Setup Guide — Rishy-09 GitHub Profile

## 1. Create or open your profile repo

Your GitHub profile README must live in a repo named exactly:

```txt
Rishy-09
```

Path:

```txt
https://github.com/Rishy-09/Rishy-09
```

## 2. Copy these files into the repo

```txt
README.md
README.source.md
.github/assets/hero-banner.png
.github/assets/build-map.svg
.github/assets/featured-systems.svg
.github/assets/tech-stack.svg
.github/assets/signals.svg
.github/workflows/profile-readme.yml
```

## 3. Commit

```bash
git add .
git commit -m "feat: redesign GitHub profile"
git push origin main
```

## 4. Pin repositories in this order

1. AI-Voice-Calling-Assistant
2. RAG_LLM
3. CtrlX_Error
4. English2PunjabiTranslator
5. kmeans-image-compression
6. GPT_2_Reproduced / minGPT-custom / Adaptive-Traffic-Signal-YOLO after cleanup

## 5. Important cleanup

Your AI voice repo link currently may not be publicly visible from outside the org.
Make sure the repository is public if you want visitors to click it.

## 6. Recommended profile bio

```txt
AI/ML + Full-Stack Engineer | Voice Agents · RAG · Transformers · MERN | Always shipping
```

## 7. Recommended repo topics

Add topics to each important repo.

### AI Voice Calling Assistant
```txt
voice-agent, local-ai, whisper, ollama, llama, piper, twilio, fastapi, vad, real-time-audio
```

### RAG_LLM
```txt
rag, fastapi, langchain, qdrant, ollama, docker, local-llm, document-qa
```

### CtrlX_Error
```txt
mern, react, nodejs, express, mongodb, bug-tracker, rbac, dashboard
```

### English2PunjabiTranslator
```txt
transformer, pytorch, nlp, translation, attention, sequence-to-sequence
```

### K-Means Image Compression
```txt
machine-learning, kmeans, image-compression, numpy, clustering
```
